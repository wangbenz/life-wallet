{
  "name": "life-wallet-h5",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test": "node --test src/mock/*.test.ts src/*.test.ts",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "lucide": "^1.24.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
